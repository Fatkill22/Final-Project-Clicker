name: final_proj
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.5.3


dependencies:
  flutter:
    sdk: flutter
  http: ^1.0.0
  in_app_purchase: ^3.2.0
  audioplayers: ^6.1.0
  cupertino_icons: ^1.0.8
  confetti: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  confetti: ^0.8.0
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.14.2

flutter_icons:
  image_path: "Assets/joke.png"
  android: true
  ios: true

flutter:


  uses-material-design: true
  assets:
    - Assets/

